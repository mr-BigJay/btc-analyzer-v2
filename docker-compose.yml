services:
  btc-analyzer:
    build: .
    container_name: btc-analyzer-v3
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    command: python -m src.main serve
